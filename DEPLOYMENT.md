# 🚀 دليل النشر - متجر السلطان

## المتطلبات

- Node.js v16 أو أحدث
- MongoDB Atlas account (قاعدة بيانات سحابية)
- Manus platform account

---

## 📋 خطوات النشر

### 1. إعداد قاعدة البيانات MongoDB

#### أ) إنشاء حساب MongoDB Atlas:
1. اذهب إلى https://www.mongodb.com/cloud/atlas
2. أنشئ حساباً جديداً أو سجل الدخول
3. أنشئ مشروعاً جديداً باسم "alsultan-tires"
4. أنشئ cluster جديد (اختر الخطة المجانية)

#### ب) الحصول على رابط الاتصال:
1. انقر على "Connect"
2. اختر "Connect your application"
3. انسخ رابط الاتصال (Connection String)
4. استبدل `<username>` و `<password>` ببيانات الاتصال الخاصة بك

#### ج) تحديث ملف .env:
```bash
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/alsultan_tires?retryWrites=true&w=majority
```

---

### 2. تحديث بيانات الأمان

#### أ) تغيير بيانات الدخول:
في ملف `.env`:
```bash
MANAGER_USERNAME=your_secure_username
MANAGER_PASSWORD=YourSecurePassword123!
```

**متطلبات كلمة المرور الآمنة:**
- 8 أحرف على الأقل
- تحتوي على أحرف كبيرة وصغيرة
- تحتوي على أرقام
- تحتوي على رموز خاصة (!@#$%^&*)

#### ب) تحديث CORS Origin:
```bash
CORS_ORIGIN=https://your-domain.com
```

#### ج) تحديث معلومات المتجر:
```bash
STORE_EMAIL=your_email@example.com
STORE_PHONE=your_phone_number
STORE_LOCATION=your_location
```

---

### 3. التحقق من الملفات المطلوبة

تأكد من وجود الملفات التالية:
- ✅ `.env` - متغيرات البيئة
- ✅ `.env.example` - مثال للمطورين
- ✅ `server.mjs` - الخادم الرئيسي
- ✅ `models/Product.mjs` - نموذج المنتج
- ✅ `models/Manager.mjs` - نموذج المدير
- ✅ `config/database.mjs` - إعدادات قاعدة البيانات
- ✅ `middleware/auth.mjs` - المصادقة والتحقق

---

### 4. اختبار محلي

```bash
# تثبيت المكتبات
npm install

# بدء الخادم
npm start
```

اختبر:
1. الصفحة الرئيسية: http://localhost:3000
2. لوحة التحكم: http://localhost:3000/admin.html
3. تسجيل الدخول بالبيانات الجديدة

---

### 5. النشر على Manus

#### أ) حفظ نقطة تفتيش:
```bash
# في واجهة Manus
1. اضغط على "Save Checkpoint"
2. أضف وصفاً: "Production deployment with MongoDB and bcrypt"
3. اضغط "Save"
```

#### ب) نشر الموقع:
```bash
# في واجهة Manus
1. اضغط على زر "Publish"
2. اختر النقطة التفتيش المحفوظة
3. اضغط "Publish"
4. انتظر اكتمال النشر
```

#### ج) الحصول على الرابط:
- سيتم إعطاؤك رابط دائم للموقع
- يمكنك ربط نطاق خاص بك

---

## 🔐 إجراءات الأمان بعد النشر

### 1. تفعيل HTTPS
- تأكد من أن الموقع يستخدم HTTPS
- استخدم شهادات SSL/TLS

### 2. مراقبة الأمان
- فعّل logging و monitoring
- راقب محاولات تسجيل الدخول الفاشلة
- قم بعمل نسخ احتياطية منتظمة

### 3. تحديثات منتظمة
- حدّث المكتبات بانتظام
- راجع تحديثات الأمان
- قم بالاختبار قبل النشر

---

## 📊 إدارة قاعدة البيانات

### النسخ الاحتياطية:
```bash
# في MongoDB Atlas:
1. اذهب إلى "Backup"
2. اضغط "Create Backup"
3. اختر "Restore" لاستعادة نسخة قديمة
```

### مراقبة الأداء:
```bash
# في MongoDB Atlas:
1. اذهب إلى "Performance Advisor"
2. راجع التوصيات
3. قم بتحسين الفهارس إذا لزم الأمر
```

---

## 🆘 استكشاف الأخطاء

### خطأ: "Cannot connect to MongoDB"
**الحل:**
1. تحقق من رابط الاتصال في `.env`
2. تأكد من أن IP الخادم مضاف في MongoDB Atlas
3. تحقق من بيانات المستخدم والكلمة المرور

### خطأ: "Authentication failed"
**الحل:**
1. تحقق من بيانات الدخول في `.env`
2. تأكد من أن المدير موجود في قاعدة البيانات
3. أعد تشغيل الخادم

### خطأ: "CORS error"
**الحل:**
1. تحقق من `CORS_ORIGIN` في `.env`
2. تأكد من أن النطاق صحيح
3. أعد تشغيل الخادم

---

## 📞 الدعم

للمساعدة:
- البريد: mosa.muad@gmail.com
- الهاتف: 0957291223
- الموقع: سوريا، ريف دمشق، داريا

---

## ✅ قائمة التحقق النهائية

- [ ] تحديث `.env` بالبيانات الصحيحة
- [ ] اختبار الاتصال بـ MongoDB
- [ ] اختبار تسجيل الدخول
- [ ] اختبار إضافة/تعديل/حذف المنتجات
- [ ] التحقق من الأداء
- [ ] حفظ نقطة تفتيش
- [ ] نشر الموقع
- [ ] اختبار الموقع المنشور
- [ ] إعداد النسخ الاحتياطية

---

**آخر تحديث:** 30 نوفمبر 2025
