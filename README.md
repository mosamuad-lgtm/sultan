# متجر إطارات السلطان

موقع ويب احترافي لمتجر إطارات "السلطان" بطابع سينمائي درامي.

## المميزات

- 🎬 **تصميم سينمائي درامي** - ألوان احترافية (أسود، ذهبي، أحمر داكن)
- 📱 **تصميم متجاوب** - يعمل على جميع الأجهزة
- 🛞 **عرض المنتجات** - واجهة جذابة لعرض الإطارات
- 🎛️ **لوحة إدارية** - التحكم الكامل في المنتجات
- ➕ **إضافة/تعديل/حذف المنتجات** - إدارة سهلة للمخزون
- 📊 **لوحة معلومات** - إحصائيات المنتجات

## البدء السريع

### المتطلبات
- Node.js 14+
- npm أو pnpm

### التثبيت

```bash
cd alsultan_tires
npm install
```

### تشغيل الخادم

```bash
npm start
```

الموقع سيكون متاحاً على `http://localhost:3000`

### الوصول إلى لوحة التحكم

اذهب إلى `http://localhost:3000/admin.html` للدخول إلى لوحة التحكم الإدارية.

## هيكل المشروع

```
alsultan_tires/
├── index.html          # الصفحة الرئيسية
├── admin.html          # صفحة لوحة التحكم
├── style.css           # أنماط الصفحة الرئيسية
├── admin-style.css     # أنماط لوحة التحكم
├── app.js              # سكريبت الصفحة الرئيسية
├── admin.js            # سكريبت لوحة التحكم
├── server.mjs          # الخادم الرئيسي
├── package.json        # معلومات المشروع والمكتبات
└── README.md           # هذا الملف
```

## API Endpoints

### المنتجات

- `GET /api/products` - الحصول على جميع المنتجات
- `GET /api/products/:id` - الحصول على منتج واحد
- `POST /api/products` - إضافة منتج جديد
- `PUT /api/products/:id` - تحديث منتج
- `DELETE /api/products/:id` - حذف منتج

## التطوير المستقبلي

- [ ] قاعدة بيانات حقيقية (MongoDB/PostgreSQL)
- [ ] نظام المصادقة والتسجيل
- [ ] سلة التسوق والدفع
- [ ] نظام التقييمات والتعليقات
- [ ] إرسال رسائل بريد إلكترونية
- [ ] نظام الفواتير والطلبات

## الترخيص

جميع الحقوق محفوظة لمتجر إطارات السلطان © 2024
